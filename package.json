{
  "name": "tenjin",
  "version": "0.1.0-rc.2",
  "description": "Tenjin — a personal agent harness. Repo codename: stealth.",
  "license": "MIT",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "tenjin": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "tenjin": "bun run src/index.ts",
    "build:release": "bun run scripts/build-release.ts",
    "release:blackbox": "bun run scripts/release-blackbox.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run typecheck"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
